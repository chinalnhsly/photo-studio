{
  "name": "photostudio",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/jwt": "^10.0.0",
    "bcrypt": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0",
    "@types/bcrypt": "^5.0.0"
  },
  "resolutions": {
    "@types/node": "^20.17.30"
  }
}
