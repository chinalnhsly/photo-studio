export enum PointLogType {
  SIGN_IN = 'sign_in',     // 签到
  PURCHASE = 'purchase',    // 购物
  REFUND = 'refund',       // 退款
  ADJUST = 'adjust',       // 调整
  EXPIRED = 'expired',     // 过期
  SYSTEM = 'system'        // 系统操作
}
